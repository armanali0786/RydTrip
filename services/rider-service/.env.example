PORT=3001
DATABASE_URL=postgresql://rydtrip:rydtrip@localhost:5433/rydtrip_riders
KAFKA_BROKERS=localhost:9094
# Shared across rider-service, driver-service, and api-gateway so the gateway
# can verify tokens either service issues. Local-dev-only value — see
# docs/adr/005-basic-auth-pulled-forward.md and Phase 11 for the real secret
# management story (Secrets Manager, per-service keys, rotation).
JWT_SECRET=dev-shared-secret-change-me
JWT_EXPIRES_IN=1d
