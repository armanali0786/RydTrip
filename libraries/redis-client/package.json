{
  "name": "@rydtrip/redis-client",
  "version": "0.0.1",
  "private": true,
  "description": "Shared ioredis wrapper and driver GEO index for RydTrip services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
