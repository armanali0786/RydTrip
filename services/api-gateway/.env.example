PORT=3000
RIDER_SERVICE_URL=http://localhost:3001
DRIVER_SERVICE_URL=http://localhost:3002
TRIP_SERVICE_URL=http://localhost:3003
# Shared across rider-service, driver-service, and api-gateway — see
# docs/adr/005-basic-auth-pulled-forward.md. Gateway only verifies tokens,
# it never signs them.
JWT_SECRET=dev-shared-secret-change-me
