{
  "name": "@rydtrip/circuit-breaker",
  "version": "0.0.1",
  "private": true,
  "description": "Lightweight in-house circuit breaker for guarding calls to flaky downstream dependencies (Phase 8)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
