PORT=3004
REDIS_URL=redis://localhost:6380
KAFKA_BROKERS=localhost:9094
# How long a driver's location stays visible to GEO search after their last
# ping before it's treated as stale — see libraries/redis-client/src/driver-geo-index.ts.
DRIVER_HEARTBEAT_TTL_SECONDS=30
