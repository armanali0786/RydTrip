PORT=3002
DATABASE_URL=postgresql://rydtrip:rydtrip@localhost:5433/rydtrip_drivers
# Shared across rider-service, driver-service, and api-gateway — see
# docs/adr/005-basic-auth-pulled-forward.md.
JWT_SECRET=dev-shared-secret-change-me
JWT_EXPIRES_IN=1d
